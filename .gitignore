# Secrets — never commit
.env
.env.*
!.env.example

# macOS
.DS_Store

# Xcode / Swift
xcuserdata/
*.xcuserstate
DerivedData/
.build/
.swiftpm/
*.ipa
*.dSYM.zip

# XcodeGen-generated project — project.yml is the source of truth.
# Run `xcodegen generate` (or scripts/bootstrap.sh) after cloning.
*.xcodeproj/
Tripto/Generated/
TriptoWidgets/Generated/

# Python (scripts/gen_tokens.py)
__pycache__/
*.pyc

# Claude Code agent worktrees — internal scratch, not project content.
.claude/worktrees/
.claude/company/
# wrangler local secrets (any worker under web/)
web/**/.dev.vars
web/**/.env
web/**/.env.*

# Node (workers under web/)
node_modules/
