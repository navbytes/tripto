# Handover — 2026-07-10

Snapshot of everything built/fixed in this session and exactly what's left
for the owner. Both repos (`tripto`, `~/repos/backend`) are on `main`,
clean, nothing pending review.

## What shipped

| # | What | Where | Status |
|---|------|-------|--------|
| 1 | UX audit cycle across all screens (11 screens, 1 cycle each) | tripto `main` | ✅ Merged (PR [#3](https://github.com/navbytes/tripto/pull/3)) |
| 2 | Trip hero header: fixed cut-off buttons + non-collapsing scroll (3 root causes: `@State` over-invalidation, hardcoded meta-row height, shared coordinate-space collision across tabs) | tripto `main` | ✅ Merged (part of PR #3) |
| 3 | Email import EI-0→EI-3: schema, `ingest-email` edge function, review-gate UI, Cloudflare Email Worker | tripto + backend `main` | ✅ Merged (PR [#4](https://github.com/navbytes/tripto/pull/4)) |
| 4 | `ingest-email` LLM routing moved to Cloudflare AI Gateway (Unified Billing, configurable `LLM_MODEL`) | backend `main` | ✅ |
| 5 | Import address made persistently accessible (was only visible on an empty itinerary; now also lives in `ShareTripView`, with proper failed/retry state) | tripto `main` | ✅ Merged (part of PR #4) |
| 6 | **Production bug found & fixed**: `trips_select` RLS policy blocked *every* trip creation that asked for the row back (`Prefer: return=representation`, which is what the app's insert-and-return call uses) | backend `main` | ✅ Fixed, verified live |
| 7 | Paste-to-import TI-0→TI-2: paste booking text or a packing list instead of forwarding email; new `ingest-text` edge function (JWT-authed, no service role) + app UI | tripto + backend `main` | ✅ Merged (PR [#5](https://github.com/navbytes/tripto/pull/5)) |
| 8 | Fixed stale docs (`ANTHROPIC_API_KEY` reference, predates the AI Gateway migration) | backend `main` | ✅ |
| 9 | Universal links entitlement | — | ⛔ Not started — blocked entirely on you (see below) |
| 10 | Cloudflare AI Gateway wired up live: Unified Billing credits loaded, working scoped API token set, three code bugs found & fixed (see below) | backend `main` | ✅ Fixed, verified live end-to-end |

## The production bug (#6) — worth understanding

Postgres re-checks a table's **SELECT** policy on `INSERT ... RETURNING`,
not just the INSERT policy. `trips`' `AFTER INSERT` trigger (which grants
the creator membership via `trip_members`, which is what `trips_select`'s
`is_trip_member(id)` check depends on) fires *after* that RETURNING check —
so a trip's own creator failed RLS on any insert that asked for the row
back, which is exactly what `supabase-swift`'s `.insert().select()` does.
Every new trip creation was silently broken in production until this was
fixed. Root-caused via live curl repro + direct SQL simulation, confirmed
fixed with a before/after live test (`403` → `201` with full row body).
Fix: `trips_select` now also allows `created_by = auth.uid()` directly, not
just `is_trip_member(id)`. Saved to memory as a pattern to watch for
elsewhere (any future "insert row, trigger grants visibility" design).

## AI Gateway (#10) — three bugs found fixing this live

1. **Wrong Anthropic model ID.** Default was `anthropic/claude-haiku-4-5`
   (hyphens); AI Gateway's catalog uses `anthropic/claude-haiku-4.5` (period
   before the version) — 404 on every call.
2. **OpenAI-shaped tool-calling doesn't round-trip for `anthropic/*` models**
   on Cloudflare's endpoints. Confirmed live: the request needs
   Anthropic-native `tools`/`tool_choice` shapes to even be accepted, and
   then the response's tool-call content isn't translated back into
   `tool_calls` at all — silently dropped. `openai/*` models round-trip
   correctly with the exact same OpenAI-shaped request our code already
   sends. Fixed by switching `DEFAULT_LLM_MODEL` to `openai/gpt-4.1-mini`
   instead of adding provider-specific branching.
3. **Wrong endpoint for scoped API tokens.** `callLLM()` called
   `api.cloudflare.com/client/v4/accounts/{id}/ai/v1/chat/completions` (the
   newer unified endpoint, ~6 weeks old as of this fix). A fine-grained
   `AI Gateway: Run` API token gets a flat `401 {code:10000}` there —
   reproduced with four separately-created tokens, one of which
   independently verified as valid/active via `/user/tokens/verify`. The
   identical token works immediately against the older
   `gateway.ai.cloudflare.com/v1/{account}/default/compat/chat/completions`
   endpoint. Broad account-owner OAuth sessions (`wrangler auth token`)
   don't hit this, which made it easy to miss while testing locally — only
   scoped tokens are affected. Switched `callLLM()` to the older endpoint.
   Revisit switching back if Cloudflare fixes scoped-token auth on the
   newer one.

All three fixed in `functions/_shared/extract.ts`, deployed, and verified
live: both `kind=booking` (multi-item extraction, 2 items from one paste)
and `kind=packing` tested end-to-end against production with a real
anonymous test user + trip, LLM call, and DB insert.

## Next steps — all yours

In rough priority order:

### 1. ~~Cloudflare AI Gateway~~ — done, see #10 above

### 2. ~~Email routing~~ — done 2026-07-11, verified end-to-end live

Shared secret generated and set identically on both sides
(`EMAIL_INGEST_SHARED_SECRET`, Supabase + Worker), Worker deployed.

The apex-vs-subdomain risk flagged in this doc was real: Cloudflare's
Email Routing "Enable" flow demands exclusive control of the root domain's
MX — there is no in-UI merge option, confirmed both by hitting the actual
error live ("Existing non-Cloudflare MX records conflict with Email
Routing") and by Cloudflare's own docs. The fix was **Settings > Subdomains**
directly: adding `plans.tripto.navbytes.io` there provisions MX/SPF scoped
only to that subdomain, without ever touching or requiring the apex to be
enabled. `navbytes.io`'s own MX (iCloud, `tripto@navbytes.io`) and SPF were
verified unchanged before and after. Catch-all rule (technically zone-wide
in the UI, but inert for the apex since only the subdomain's MX points at
Cloudflare) → Send to Worker → `tripto-email`, now Active.

Verified live: a real email sent to `t-<token>@plans.tripto.navbytes.io`
against a throwaway test trip produced a `'suggested'` itinerary item
(`AA100 SFO to JFK`, source `email_import`) within seconds. Test trip and
item cleaned up after.

### ~~3. Universal links~~ — done 2026-07-11

Associated Domains capability enabled on App ID `io.navbytes.tripto` (Apple
Developer console). App-side: `com.apple.developer.associated-domains:
[applinks:tripto.navbytes.io]` added to `project.yml`'s entitlements,
`xcodegen generate` run, and a real signed Release archive confirmed clean
(`** ARCHIVE SUCCEEDED **`, automatic signing regenerated the provisioning
profile with the new capability with no manual step). AASA was already
correct on the web side. `https://tripto.navbytes.io/join/<token>` now opens
the app directly.

### 4. Cloudflare MCP connector (optional, for me)
Authorized 2026-07-11 (confirmed by owner), but the account-level tools
(`cloudflare-api`/`cloudflare-bindings`/`cloudflare-builds`/`cloudflare-observability`)
still aren't showing up in the session where the grant happened — confirmed
this really is the "needs a fresh session to attach" caveat, not a stale
grant. Start a new session in this project to pick it up.

### ~~5. Once #2 is done~~ — done, see #2 above

## Reference

- Plan doc: [`docs/EMAIL_IMPORT_PLAN.md`](EMAIL_IMPORT_PLAN.md) — has its own
  (now slightly stale) owner checklist; this document supersedes it for
  current status.
- Release readiness: [`docs/RELEASE_READINESS.md`](RELEASE_READINESS.md).
- Backend repo secrets/migration workflow: `~/repos/backend/CLAUDE.md` and
  `~/repos/backend/projects/tripto/functions/README.md`.
